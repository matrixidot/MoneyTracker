<Window x:Class="MoneyTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:dat="clr-namespace:System.Windows.Data;assembly=PresentationFramework"
        xmlns:vm="clr-namespace:MoneyTracker.ViewModels"
        xmlns:models="clr-namespace:MoneyTracker.Models"
        xmlns:conv="clr-namespace:MoneyTracker.Converters"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="MoneyTracker"
        Height="900"
        Width="1300"
        Background="#F6F7FB"
        TextElement.Foreground="#111827"
        WindowStartupLocation="CenterScreen">

    <!-- ViewModel -->
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <!-- Resources -->
    <Window.Resources>

        <!-- KPI card style -->
        <Style x:Key="KpiCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>


        <!-- Enum values for Type dropdown -->
        <dat:ObjectDataProvider x:Key="TransactionTypes"
                                MethodName="GetValues"
                                ObjectType="{x:Type sys:Enum}">
            <dat:ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:TransactionType"/>
            </dat:ObjectDataProvider.MethodParameters>
        </dat:ObjectDataProvider>

        <!-- Category display converter -->
        <conv:CategoryIdToNameConverter x:Key="CategoryIdToNameConverter"/>

        <!-- Invalid row highlight -->
        <Style x:Key="TransactionRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsValid}" Value="False">
                    <Setter Property="Background" Value="#33FF0000"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <DockPanel Margin="16">

        <!-- Toolbar -->
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,12">

            <Button Content="Add"
                    Width="80"
                    Margin="0,0,8,0"
                    Command="{Binding AddRowCommand}"/>

            <Button Content="Delete"
                    Width="80"
                    Margin="0,0,8,0"
                    Command="{Binding DeleteRowCommand}"/>

            <Button Content="Save"
                    Width="80"
                    Margin="0,0,16,0"
                    Command="{Binding SaveCommand}"/>            
            
            <Button Content="Refresh"
                    Width="80"
                    Margin="0,0,16,0"
                    Command="{Binding RefreshCommand}"/>

            <Separator Margin="12,0,12,0" Width="1"/>

            <TextBlock Text="Category:"
                       VerticalAlignment="Center"
                       Margin="0,0,6,0"
                       Opacity="0.7"/>

            <TextBox Width="160"
                     Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0"/>

            <Button Content="Add Category"
                    Command="{Binding AddCategoryCommand}"/>

            
            <TextBlock Text="Month:"
                       VerticalAlignment="Center"
                       Margin="10,0,6,0"/>

            <DatePicker Width="140"
                        SelectedDate="{Binding SelectedMonth,
                                       Mode=TwoWay,
                                       UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{Binding StatusText}"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0"
                       Opacity="0.7"/>
        </StackPanel>

        <!-- Main Layout -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- KPI cards -->
                <RowDefinition Height="320"/>   <!-- Insights (charts) -->
                <RowDefinition Height="*"/>     <!-- Transactions -->
            </Grid.RowDefinitions>


            <!-- KPI Cards -->
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="0,0,0,16">

                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="Income" Opacity="0.6"/>
                        <TextBlock Text="{Binding MonthIncome, StringFormat=C}"
                                   FontSize="22"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="Expense" Opacity="0.6"/>
                        <TextBlock Text="{Binding MonthExpense, StringFormat=C}"
                                   FontSize="22"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource KpiCard}">
                    <StackPanel>
                        <TextBlock Text="Net" Opacity="0.6"/>
                        <TextBlock Text="{Binding MonthNet, StringFormat=C}"
                                   FontSize="22"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

            </StackPanel>

            <!-- Insights -->
            <Grid Grid.Row="1"
                  Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Pie Chart -->
                <Border Grid.Column="0" Style="{StaticResource KpiCard}" MinHeight="280">
                    <lvc:PieChart Series="{Binding ExpensePieSeries}"
                                  LegendPosition="Bottom"/>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource KpiCard}" MinHeight="280">
                    <lvc:CartesianChart Series="{Binding TrendSeries}"
                                        XAxes="{Binding TrendXAxes}"/>
                </Border>

            </Grid>

            <!-- Transactions (secondary) -->
            <Expander Grid.Row="2"
                      Header="Transactions"
                      IsExpanded="False">

                <Border Style="{StaticResource KpiCard}"
                        Margin="0,8,0,0">

                    <DataGrid ItemsSource="{Binding Rows}"
                              SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="False"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              HeadersVisibility="Column"
                              GridLinesVisibility="All"
                              RowStyle="{StaticResource TransactionRowStyle}">

                        <DataGrid.Columns>

                            <!-- Date -->
                            <DataGridTemplateColumn Header="Date" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Date, StringFormat=yyyy-MM-dd}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <DatePicker SelectedDate="{Binding Date, Mode=TwoWay}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Type -->
                            <DataGridTemplateColumn Header="Type" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Type}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding Source={StaticResource TransactionTypes}}"
                                                  SelectedItem="{Binding Type, Mode=TwoWay}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Category -->
                            <DataGridTemplateColumn Header="Category" Width="220">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource CategoryIdToNameConverter}">
                                                    <Binding Path="CategoryId"/>
                                                    <Binding Path="DataContext.Categories"
                                                             RelativeSource="{RelativeSource AncestorType=Window}"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding DataContext.Categories,
                                                                       RelativeSource={RelativeSource AncestorType=Window}}"
                                                  DisplayMemberPath="Name"
                                                  SelectedValuePath="Id"
                                                  SelectedValue="{Binding CategoryId, Mode=TwoWay}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Name -->
                            <DataGridTextColumn Header="Name"
                                                Width="*"
                                                Binding="{Binding Name, Mode=TwoWay}"/>

                            <!-- Cost -->
                            <DataGridTextColumn Header="Cost"
                                                Width="120"
                                                Binding="{Binding Cost, Mode=TwoWay, StringFormat=F2}"/>

                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Expander>

        </Grid>
    </DockPanel>
</Window>
